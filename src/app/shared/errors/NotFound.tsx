import React from 'react';
import { RouteComponentProps } from '@reach/router';

const NotFound: React.FC<RouteComponentProps> = () => (
  <div>
    This page not found, so fuck off...
  </div>
);

export default NotFound;
